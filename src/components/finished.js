import React from 'react';

class Finished extends React.Component {
    render() {
        return (
            <span style={ {display: `none`} }>Finished!!</span>
        );
    }
}

export default Finished;